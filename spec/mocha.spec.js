// Canonical REST
// Mocha Specification Suite

// Imports
import { assertDeepStrictEqual } from 'assert-deep-strict-equal';

// Setup

////////////////////////////////////////////////////////////////////////////////
describe('Something...', () => {

   it('is correct', () => {
      const actual =   { something: true };
      const expected = { something: true };
      assertDeepStrictEqual(actual, expected);
      });

   });
